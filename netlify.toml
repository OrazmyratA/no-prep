[build]
  command = "npm run build:prod"
  publish = "dist/no-prep/browser"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — hash routing handles routes client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
