[package]
name = "security-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "2"
napi-derive = "2"
rsa = "0.9"
sha2 = { version = "0.10", features = ["oid"] }
base64 = "0.22"
hex = "0.4"

[build-dependencies]
napi-build = "2"
